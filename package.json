{
  "name": "patterns",
  "version": "1.1.0",
  "scripts": {
    "test": "NODE_PATH=. node --test --experimental-test-module-mocks",
    "types": "tsc -p ./tsconfig.json",
    "lint": "eslint .; prettier -c \"**/*.js\"",
    "fix": "eslint . --fix; prettier --write \"**/*.js\"",
    "app": "NODE_PATH=. node app/index.js"
  },
  "author": "Artem Yemelianov",
  "private": true,
  "dependencies": {
    "eslint": "^9.12.0",
    "eslint-config-metarhia": "^9.1.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
